categories:
  - id: sdk
    name: Psy-Q SDK
    paths:
      - slps_023.64/psyq
  - id: main
    name: Main Executable
    paths:
      - slps_023.64

expected_paths:
  asm: expected/asm
  src: expected/src

ignored_files:
  - slps_023.64/header

ssh:
  enabled: false                                     # Set to true to enable SSH builds
  host: "192.168.64.8"                               # VM hostname or IP address
  user: "jdperos"                                    # SSH username (leave empty to use current user)
  port: 22                                           # SSH port (default: 22)
  remote_path: "/home/jdperos/chrono-cross-decomp"   # Absolute path to project on VM
  identity_file: ""                                  # Optional: SSH key path (e.g., "~/.ssh/id_ed25519")

scratch:
  enabled: true
  platform: ps1
  compiler: gcc2.8.1-psx
  c_flags: "-Wall -Wa,--aspsx-version=2.79,--expand-div -g3 -O2 -G0"
  ctx_path: ctx/project.ctx.c
  build_ctx: true

output: ./objdiff.json
